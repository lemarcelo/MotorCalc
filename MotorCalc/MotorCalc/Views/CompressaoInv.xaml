<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MotorCalc.Views.CompressaoInv">
    <ContentPage
        Shell.NavBarIsVisible="False">
        <ContentPage.Content>
            <StackLayout VerticalOptions="StartAndExpand">
                <Label Text="CALCULO Compressão"
                       FontSize="Title"
                       VerticalOptions="CenterAndExpand" 
                       HorizontalOptions="CenterAndExpand" />
                <Label Text="{Binding ResultadoCompr, Mode=TwoWay, StringFormat='{0:F2}' }"
                       FontSize="80"
                       TextColor="{StaticResource Primary}"
                       VerticalOptions="CenterAndExpand" 
                       HorizontalOptions="CenterAndExpand" />
                <Entry x:Name="entryCilindrada" HorizontalTextAlignment="Center" FontAttributes="Bold" Keyboard="Numeric" Placeholder="Cilindrada" FontSize="Title" Margin="100,0,100,20" Text="{Binding Cilindrada}"/>
                <Entry x:Name="entryVolume" HorizontalTextAlignment="Center" FontAttributes="Bold" Keyboard="Numeric" Placeholder="Volume" FontSize="Title" Margin="100,0,100,40" Text="{Binding Volume}"/>
                <Button Text="Calcular" Margin="40,0,40,0" Command="{Binding CompressaoCalc}"/>
            </StackLayout>
        </ContentPage.Content>
    </ContentPage>
</Shell>